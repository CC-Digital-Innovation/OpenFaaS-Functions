version: 1.0
provider:
  name: openfaas
  gateway: http://127.0.0.1:8080
functions:
  sandbox:
    lang: python3
    handler: ./sandbox
    image: afarina96/sandbox:latest
    secrets:
    - test-secret
